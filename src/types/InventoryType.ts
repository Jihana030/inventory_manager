export type InventoryType = {
    id : number;
    name : string;
    category : string;
    option_name : string;
    thumbnail: string;
    count: number;
    safe_count: number;
    current_buy: string;
    memo: string;
};